---
import { siteConfig } from '../lib/site-config';
import AviationEditorialLegal from '../templates/aviation-editorial/pages/LegalPage.astro';

if (siteConfig.template !== 'aviation-editorial') {
  throw new Error(`No legal page registered for template "${siteConfig.template}".`);
}
---

<AviationEditorialLegal document="tos" />
