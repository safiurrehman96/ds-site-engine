---
import AviationEditorialAbout from '../templates/aviation-editorial/pages/AboutPage.astro';
import { siteConfig } from '../lib/site-config';

if (siteConfig.template !== 'aviation-editorial') {
  throw new Error(`No about page registered for template "${siteConfig.template}".`);
}
---

<AviationEditorialAbout />
