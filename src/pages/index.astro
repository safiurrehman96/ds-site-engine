---
import AviationEditorialHome from '../templates/aviation-editorial/pages/HomePage.astro';
import { siteConfig } from '../lib/site-config';

if (siteConfig.template !== 'aviation-editorial') {
  throw new Error(`No homepage registered for template "${siteConfig.template}".`);
}
---

<AviationEditorialHome />
