---
import { siteConfig } from '../lib/site-config';
import AviationEditorialNotFound from '../templates/aviation-editorial/pages/NotFoundPage.astro';

if (siteConfig.template !== 'aviation-editorial') {
  throw new Error(`No 404 page registered for template "${siteConfig.template}".`);
}
---

<AviationEditorialNotFound />
